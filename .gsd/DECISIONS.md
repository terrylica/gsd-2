# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001-1ya5a3 | arch | Desktop shell framework | Electron + Vite | Chromium-based (required for Monaco, Shiki, potential future WebContainers), mature ecosystem, battle-tested for code editors (VS Code, Cursor). Electrobun considered but too young and needs CEF for Chromium. Tauri has system webview limitations. | Yes — if Electrobun matures |
| D002 | M001-1ya5a3 | arch | Web preview approach | Localhost iframe | gsd-2 already spawns local dev servers. iframe is simpler, zero overhead, real environment. WebContainers add complexity for a local app with full filesystem access. | Yes — if sandboxing needed |
| D003 | M001-1ya5a3 | library | Code editor | Monaco Editor (@monaco-editor/react) | Powers VS Code. Full language support, IntelliSense, diff views. Custom theming via defineTheme(). CodeMirror lighter but less out-of-box for a coding app. | No |
| D004 | M001-1ya5a3 | library | UI primitives | Radix + Tailwind (custom components) | Headless, accessible primitives with zero visual opinions. Total design control. shadcn/ui has recognizable aesthetic that conflicts with "no AI slop" requirement. | No |
| D005 | M001-1ya5a3 | convention | Icon set | Phosphor Icons | Minimal, geometric, consistent stroke weight. Used by Linear, Vercel. Anti-Lucide — explicitly chosen to avoid the generic AI app aesthetic. | No |
| D006 | M001-1ya5a3 | convention | Typography | Inter (UI) + JetBrains Mono (code) | Inter has great optical sizing and tabular figures. JetBrains Mono has ligatures and clear character distinction. Both high-quality and widely respected. | No |
| D007 | M001-1ya5a3 | convention | Color palette | Dark monochrome + warm amber/gold accent | Dark-first, terminal-inspired but refined. Single warm accent color. No purple. Monochrome grays for hierarchy. | Yes — accent color may evolve |
| D008 | M001-1ya5a3 | convention | Message layout | Continuous document flow, left-aligned | Not chat bubbles. Not turn-based blocks. A living document that grows — more like reading a premium terminal transcript than texting. | No |
| D009 | M001-1ya5a3 | library | Syntax highlighting (messages) | Shiki | TextMate grammar-based, accurate highlighting, theme customization. Used for code blocks in the markdown message stream. Monaco handles the editor separately. | No |
| D010 | M001-1ya5a3 | library | State management | Zustand | Lightweight, minimal boilerplate, works well with React. No Redux overhead for a desktop app. | Yes — if state complexity grows |
| D011 | M001-1ya5a3 | arch | Layout model | Three-column resizable | File tree (left), conversation (center), editor+preview (right). Draggable dividers. Center is primary focus. Panels collapsible. | No |
| D012 | M001-1ya5a3/S01 | library | Electron/Vite compatibility baseline | electron-vite v5 + @vitejs/plugin-react v5 | electron-vite v5 peers through Vite 7; forcing the newer React plugin/Vite 8 line breaks install and dev bootstrap. | Yes — when electron-vite upgrades |
| D013 | M001-1ya5a3/S01 | pattern | Resizable panel persistence contract | react-resizable-panels v4 `Group`/`Panel`/`Separator` + `useDefaultLayout` | The installed library API differs from older notes. This is the real persistence path and the saved localStorage key is observable for verification. | No |
| D014 | M001-1ya5a3/S01 | design | Shell chrome hierarchy | Panel shells own the heavy chrome; nested cards stay flatter and lower-contrast | Live review showed over-rounded cards read like a generic component kit. Flattening nested surfaces preserves the desktop-tool aesthetic. | Yes — if later slices prove a better hierarchy |
| D015 | M001-1ya5a3/S02 | pattern | Extension UI auto-response during S02 | Auto-respond to interactive extension_ui_request methods (select→first option, confirm→true, input→empty, editor→prefill) with console.warn | Prevents agent from blocking indefinitely before S05 builds the real interactive prompt UI. Warnings make it visible during development. Fire-and-forget methods (notify, setStatus, setWidget, setTitle, set_editor_text) are forwarded as events without response. | Yes — S05 replaces auto-response with real prompt UI |
| D016 | M001-1ya5a3/S03 | library | Streaming markdown renderer | Streamdown v2.5.0 + @streamdown/code v1.1.1 with Shiki (vitesse-dark) | Streamdown provides block-level memoization for streaming — only changed blocks re-render. Built-in caret animation. @streamdown/code integrates Shiki for syntax highlighting with lazy WASM loading. Custom component overrides give full design control. | No |
| D017 | M001-1ya5a3/S03 | pattern | Component override typing for react-jsx tsconfig | ComponentPropsWithoutRef<T> & ExtraProps instead of JSX.IntrinsicElements | react-jsx tsconfig mode doesn't expose the global JSX namespace, causing TS2503 errors. ComponentPropsWithoutRef is functionally equivalent and avoids the namespace issue. | No |
| D018 | M001-1ya5a3/S03 | pattern | Streaming caret scoping | Only the final assistant-text block shows the caret (isLastBlock via backward scan) | Multiple assistant blocks can exist in a conversation (interrupted by tools). The caret should only animate on the newest/active block to avoid confusing visual signals. | No |
| D019 | M001-1ya5a3/S04 | pattern | Tool card expand/collapse animation | CSS grid-template-rows 0fr↔1fr transition, children always rendered | JS height measurement alternatives are complex and fragile. CSS grid-rows gives smooth 300ms animation but requires children to always be in the DOM (not conditionally rendered) so the grid can measure content height. | No |
| D020 | M001-1ya5a3/S04 | pattern | Syntax highlighting inside tool cards | Wrap content in markdown code fence, render via Streamdown+codePlugin | Avoids calling codeToHtml directly or creating a new Shiki integration path. Reuses the existing Streamdown pipeline with zero new wiring. Established pattern for WriteCard and ReadCard. | No |
| D021 | M001-1ya5a3/S04 | pattern | Tool card dispatcher routing | Switch statement with case-insensitive aliases | Clearer than a map for ~12 cases. Allows natural alias grouping (e.g., 'bash'/'async_bash'→BashCard, 'read'/'Read'→ReadCard). GenericCard as default case provides crash-proof fallback for unknown tools. | No |
| D022 | M001-1ya5a3/S04 | pattern | Intra-line diff highlighting | Batch consecutive removed→added lines, apply Diff.diffWords() on 1:1 pairs | Same algorithm as the TUI diff.ts reference. Single-pass parser collects removed/added batches. When exactly one removed pairs with one added, word-level tokens are computed. Changed tokens get stronger bg highlights within the line-level coloring. | No |
